# Core: CLI (run.py), OAuth helper (clio_oauth_app.py)
requests>=2.31.0
python-dotenv>=1.0.0
flask>=3.0.0
pyOpenSSL>=24.0.0

# Web API: FastAPI backend for the React "Clio Data Management" UI (uvicorn on :8000)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 is incompatible with bcrypt 4.1+ (removed __about__); keep 4.0.x until auth is refactored
bcrypt==4.0.1
python-multipart>=0.0.9
